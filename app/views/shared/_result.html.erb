<li class="<%= 'new' if result.most_recent? -%>">
  <% result.teams.each_with_index do |team, i| %>
    <% team.players.each do |winner| %>
      <%= link_to winner, class: 'player' do %>
        <%= image_tag(gravatar_url(winner, size: 16)) %>
        <strong> <%= winner.name %></strong>
      <% end %>
      <% swing = result.elo_swing(winner) %>
      <% if swing.positive? %>
        <elo-swing class="positive">+<%= swing %></elo-swing>
      <% else %>
        <elo-swing class="negative"><%= swing %></elo-swing>
      <% end %>
    <% end %>
    <% if i == 0 %>
      <% if result.tie? %>
        <small>ties</small>
      <% else %>
        <small>defeats</small>
      <% end %>
    <% end %>
  <% end %>
  (<%= result.for %> - <%= result.against %>)
  <% if show_game %>
    in
    <strong><%= link_to result.game.name, result.game %></strong>
  <% end %>
  <time><%= format_time(result.created_at) %></time>
  <% if result.most_recent? -%>
    <%= link_to "Delete", game_result_path(result.game, result), class: "btn btn-danger", method: :delete, confirm: "Are you sure?" %>
  <% end -%>
</li>
